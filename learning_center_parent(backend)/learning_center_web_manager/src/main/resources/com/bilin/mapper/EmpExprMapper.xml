<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bilin.mapper.EmpExprMapper">
    <insert id="insertBatch">
        insert into emp_expr (emp_id, begin, end, company, job_position) values
        <foreach collection="exprList" item="expr" separator=",">
            (#{expr.empId}, #{expr.begin}, #{expr.end}, #{expr.company}, #{expr.jobPosition})
        </foreach>
    </insert>

    <delete id="deleteBatch">
        delete from emp_expr where emp_id in
        <foreach collection="empIds" separator="," item="empId" open="(" close=")">
            #{empId}
        </foreach>
    </delete>

    <select id="getByEmpId" resultType="com.bilin.entity.EmpExpr">
        select * from emp_expr where emp_id=#{empId}
    </select>

    <delete id="deleteByEmpId">
        delete from emp_expr where emp_id=#{empId}
    </delete>

</mapper>