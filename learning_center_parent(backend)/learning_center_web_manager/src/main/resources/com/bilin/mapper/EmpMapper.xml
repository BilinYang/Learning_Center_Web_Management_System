<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bilin.mapper.EmpMapper">
    <select id="page" resultType="com.bilin.entity.Emp">
        select e.*, d.name deptName from emp e
            left join dept d on e.dept_id=d.id
        <where>
            <if test="name != null and name != ''">
                e.name like concat('%', #{name}, '%')
            </if>
            <if test="gender!=null">
                and e.gender=#{gender}
            </if>
            <if test="begin!=null and end!=null">
                and e.entry_date between #{begin} and #{end}
            </if>
        </where>

        order by e.update_time desc
    </select>

    <delete id="deleteBatch">
        delete from emp where id in
        <foreach collection="ids" separator="," item="id" open="(" close=")">
            #{id}
        </foreach>
    </delete>

<!--    Method 1-->
<!--    <resultMap id="empResultMap" type="com.bilin.entity.Emp" autoMapping="true">-->
<!--        <id column="id" property="id"></id>-->
<!--        <collection property="exprList" ofType="com.bilin.entity.EmpExpr">-->
<!--            <result column="ee_id" property="id"></result>-->
<!--            <result column="ee_empId" property="empId"></result>-->
<!--            <result column="ee_begin" property="begin"></result>-->
<!--            <result column="ee_end" property="end"></result>-->
<!--            <result column="ee_company" property="company"></result>-->
<!--            <result column="ee_job" property="jobPosition"></result>-->
<!--        </collection>-->
<!--    </resultMap>-->

<!--    <select id="getById" resultMap="empResultMap">-->
<!--        select e.*, ee.id ee_id, ee.emp_id ee_empId, ee.begin ee_begin, ee.end ee_end, ee.company ee_company, ee.job_position ee_job_position-->
<!--        from emp e left join emp_expr ee on e.id=ee.emp_id where e.id=#{id}-->
<!--    </select>-->

    <update id="update">
        update emp
        <set>
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="job != null">job = #{job},</if>
            <if test="salary != null">salary = #{salary},</if>
            <if test="image != null and image != ''">image = #{image},</if>
            <if test="entryDate != null">entry_date = #{entryDate},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

    <select id="countEmpJobData" resultType="java.util.Map">
        select
            (case job when 1 then 'Class teacher'
                      when 2 then 'Lecturer'
                      when 3 then 'Student Affairs Supervisor'
                      when 4 then 'Teaching Research Supervisor'
                      when 5 then 'Counselor'
                      else 'Other' end) as pos,
            count(*) as total
        from emp group by job
        order by total
    </select>

    <select id="countEmpGenderData" resultType="java.util.Map">
        select
            if (gender=1, 'Male', 'Female') as name,
            count(*) as value
        from emp group by gender
    </select>


</mapper>